import React from "react";
import ReactDOM from "react-dom/client";

const root = ReactDOM.createRoot(document.getElementById("root"));
root.render(
  <React.StrictMode>
    <h1>✅ EasyTask is Live!</h1>
    <p>Edit <code>src/index.js</code> to get started.</p>
  </React.StrictMode>
);
